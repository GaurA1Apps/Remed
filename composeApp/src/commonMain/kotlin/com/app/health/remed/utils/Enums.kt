package com.app.health.remed.utils

enum class MedicineType { PILL, SYRUP, INJECTION, DROPS, INHALER, OTHER }

enum class FrequencyType { TIMES_PER_DAY, EVERY_N_HOURS, WEEKDAYS }

enum class DoseStatus { SCHEDULED, TAKEN, SKIPPED }

